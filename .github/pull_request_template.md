<!-- What changed, and why. -->

<!-- For a change to speed or ratio: the numbers before and after, from
the same machine and thread count, and the command that made them. -->

- [ ] `cargo test --release --workspace` passes.
- [ ] Anything written by an earlier version still decodes (a format change comes with a fixture in `tests/data/legacy/` and a line in `docs/spec.md`).
- [ ] Every commit carries `Signed-off-by:` (`git commit -s`), accepting [CLA.md](../CLA.md).
